pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo 'Clarusway_Way to Reinvent Yourself'
                sh 'echo ayhan Integrating Jenkins Pipeline with GitHub Webhook using Jenkinsfile'
            }
        }
    }
}